{
  "name": "daily-hotboard-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily hotboard dashboard and voiceover material generator powered by UApiPro hotboard data.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build --base=./ && node scripts/copy-data.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "fetch": "node scripts/fetch-hotboard.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
